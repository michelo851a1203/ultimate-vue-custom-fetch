import fs from 'fs';
import { describe, it, expect } from 'vitest';
import { useApi }  from '@demo-api-infra/custom-fetch';

describe('testing with auth usage(JWT)', () => {
  const apiUtils = useApi();

  it('', () => {

  })

});
